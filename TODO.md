# TODO

- RNAstructure bridge should grab CTDs - needed for fuzzing.
- Remove baseopt backend, unify with base (remove common, etc), and use
  templated arguments on algorithms for EnergyCfg etc.
- Convert partition function to use normal dynamic programming tables instead of sparse ones
- need to fix multiloop_c and unpaired in sparse algo. try all positive multiloop_c and pf unpaired.
